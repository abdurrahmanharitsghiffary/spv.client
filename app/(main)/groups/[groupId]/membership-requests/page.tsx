import GarPage from "@/components/page/gar-page";
import React from "react";

export const metadata = {
  title: `Membership requests`,
  keywords:
    "Application request, join request, membership request, member, request",
  category: "Membership requests",
  description: "Group membership requests",
};

export default function ApplicationRequestPage() {
  return <GarPage />;
}
